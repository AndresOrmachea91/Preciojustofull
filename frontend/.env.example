# Copiar a frontend/.env.local (Vite lo lee solo; .env.local NO se sube al repositorio).
#
# "http"    usa la API real (levantar el backend primero)
# "memoria" corre sin backend, con datos fijos: es el plan B de la demo
VITE_ORIGEN_DATOS=http
VITE_API_URL=http://localhost:8000/api/v1
